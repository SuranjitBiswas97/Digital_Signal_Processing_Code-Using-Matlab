%lab task 1: UnitFunction
close all
clear
clc


a=1;
fs=1;
t=-10:1/fs:10;


imp1=zeros(1,length(t));
for i=1:length(t);
    if t(i)==0
        imp1(i)=1;
    else  imp1(i)=0;
    end
end
figure 
stem(t,imp1);