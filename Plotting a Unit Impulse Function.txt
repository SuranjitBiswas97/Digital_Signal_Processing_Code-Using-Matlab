Task 1
Plotting a Unit Impulse Function

% Unit Impulse function
close all
clear
clc

a=1;   %Defining amplitude
fs=1;

t=-10:1/fs:10;
impl=zeros(1,length(t));
for i=1:length(t)
    if t(i)==0
        impl(i)=1;
    else
        impl(i)=0;
    end
end

figure ;
stem(t,impl)