Task 2
Plotting a Unit Ramp Function

 % Unit ramp function
close all
clear
clc 

a=1;   %Defining amplitude
fs=1;

t=-10:1/fs:10;
rmp=zeros(1,length(t));
for i=1:length(t)
    if t(i)>=0
        rmp(i)=t(i);
    else
        rmp(i)=0;
    end
end

figure ;
stem(t,rmp)